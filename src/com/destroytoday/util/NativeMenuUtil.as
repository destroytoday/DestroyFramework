package com.destroytoday.util
{
	import flash.display.NativeMenu;
	import flash.display.NativeMenuItem;

	public class NativeMenuUtil
	{
		/*public static function getItemByLabel(menu:NativeMenu, label:String):NativeMenuItem
		{
			for each (var item:NativeMenuItem in menu.items)
			{
				if (item.label == label)
				{
					return item;
				}
			}
			
			return null;
		}*/
	}
}